export const ARIA_LABEL = {
  CLOSE: "Close sidebar",
  // Names the drawer itself. Its content is consumer-supplied and has no
  // heading of its own, so without this the dialog is announced unnamed.
  SIDEBAR: "Sidebar",
} as const;
