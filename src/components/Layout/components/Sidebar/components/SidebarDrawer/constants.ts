export const ARIA_LABEL = {
  CLOSE: "Close sidebar",
  // Default name for the drawer itself. Its content is consumer-supplied and
  // has no heading of its own, so without a name the dialog is announced
  // unnamed. Consumers that know what the drawer holds pass their own label.
  SIDEBAR: "Sidebar",
} as const;
